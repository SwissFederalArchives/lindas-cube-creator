name: Deploy to PROD

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag to deploy (e.g., int-latest, test-1.0.0)'
        required: true
        type: string
        default: 'int-latest'
      component:
        description: 'Component to deploy'
        required: true
        type: choice
        options:
          - all
          - app
          - api
          - cli

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/swissfederalarchives/lindas-cube-creator

jobs:
  deploy:
    name: Deploy to PROD
    runs-on: ubuntu-latest
    environment: production

    permissions:
      packages: write

    steps:
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine components
        id: components
        run: |
          if [[ "${{ github.event.inputs.component }}" == "all" ]]; then
            echo "components=app api cli" >> $GITHUB_OUTPUT
          else
            echo "components=${{ github.event.inputs.component }}" >> $GITHUB_OUTPUT
          fi

      - name: Deploy images
        run: |
          VERSION="${{ github.event.inputs.version }}"

          for component in ${{ steps.components.outputs.components }}; do
            IMAGE="${IMAGE_PREFIX}-${component}"

            echo "Deploying ${component} to PROD..."

            # Save current prod-latest as prod-previous
            if docker pull ${IMAGE}:prod-latest 2>/dev/null; then
              echo "Saving current prod-latest as prod-previous"
              docker tag ${IMAGE}:prod-latest ${IMAGE}:prod-previous
              docker push ${IMAGE}:prod-previous
            else
              echo "No existing prod-latest tag to save"
            fi

            # Pull source image and retag as prod-latest
            echo "Pulling ${IMAGE}:${VERSION}"
            docker pull ${IMAGE}:${VERSION}
            docker tag ${IMAGE}:${VERSION} ${IMAGE}:prod-latest
            docker push ${IMAGE}:prod-latest

            echo "Tagged ${component}:${VERSION} as prod-latest"
          done

      - name: Summary
        run: |
          echo "## Deployed to PROD" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Source version:** \`${{ github.event.inputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Components:** ${{ steps.components.outputs.components }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Tag |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|-----|" >> $GITHUB_STEP_SUMMARY
          for component in ${{ steps.components.outputs.components }}; do
            echo "| ${component} | \`prod-latest\` |" >> $GITHUB_STEP_SUMMARY
          done
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "To rollback: Run **Rollback** workflow with environment=prod" >> $GITHUB_STEP_SUMMARY
